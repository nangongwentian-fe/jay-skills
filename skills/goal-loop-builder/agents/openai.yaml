interface:
  display_name: "Goal Loop Builder"
  short_description: "生成文件引用式 /goal prompt 和可验证的 loop 契约"
  default_prompt: "Use $goal-loop-builder to create a short file-backed /goal prompt or copy-ready /loop contract."
